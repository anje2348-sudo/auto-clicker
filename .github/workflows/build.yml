name: Build Windows EXE

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyautogui opencv-python pillow pyinstaller pywin32

      - name: Build EXE
        run: |
          python -m PyInstaller `
            --onefile `
            --windowed `
            --name "放置天堂_自動傳送" `
            --hidden-import pyautogui `
            --hidden-import cv2 `
            --hidden-import PIL `
            --hidden-import PIL.Image `
            --hidden-import PIL.ImageTk `
            --hidden-import pyscreeze `
            --hidden-import mouseinfo `
            auto_clicker_ui.py

      - name: Upload EXE
        uses: actions/upload-artifact@v4
        with:
          name: 放置天堂_自動傳送
          path: dist/放置天堂_自動傳送.exe
          retention-days: 30
